info:
  name: Residual Messages
  type: http
  seq: 6

http:
  method: GET
  url: "{{btp-host}}/runs/{{runId}}/residual"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
