info:
  name: Show Message
  type: http
  seq: 2

http:
  method: GET
  url: "{{btp-host}}/runs/{{runId}}/messages/{{messageId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
