info:
  name: Show Message Header
  type: http
  seq: 3

http:
  method: GET
  url: "{{btp-host}}/runs/{{runId}}/messages/{{messageId}}/header?seq=1"
  params:
    - name: seq
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
