info:
  name: Delete Run
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{btp-host}}/runs/{{runId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
