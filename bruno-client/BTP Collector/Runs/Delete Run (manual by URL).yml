info:
  name: Delete Run (manual by URL)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{btp-host}}/runs/your-testrun-id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
