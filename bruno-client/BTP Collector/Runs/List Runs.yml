info:
  name: List Runs
  type: http
  seq: 1

http:
  method: GET
  url: "{{btp-host}}/runs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
